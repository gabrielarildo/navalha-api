namespace TodoApi.Models.Entities
{
    // Status do ciclo de vida de um agendamento na barbearia Navalha
    public enum StatusAgendamento
    {
        Agendado = 0,
        Confirmado = 1,
        Concluido = 2,
        Cancelado = 3
    }
}
